@model IEnumerable<QuanLyDoAn.Models.LoaiMonHoc>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<h2>Index</h2>

<p>
    <button class="btn btn-outline-primary mt-3">@Html.ActionLink("Thêm Mới", "Create", null, new { @class = "headerButton" })</button>
</p>
<table class="table table-bordered text-wrap">
    <tr class="table-primary">
        <th>
            @Html.DisplayNameFor(model => model.TenLoaiMonHoc)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenLoaiMonHoc)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.IdLoaiMonHoc }) |
                @Html.ActionLink("Details", "Details", new { id = item.IdLoaiMonHoc }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.IdLoaiMonHoc })
            </td>
        </tr>
    }

</table>
